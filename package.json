{
  "private": true,
  "scripts": {
    "build": "pulp build -- --censor-lib --strict",
    "test": "pulp test"
  },
  "devDependencies": {
    "pulp": "^11.0.0",
    "purescript": "^0.11.4",
    "purescript-psa": "^0.5.0"
  },
  "dependencies": {
    "canvas-prebuilt": "^1.6.5-prerelease.1"
  }
}
